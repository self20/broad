dependencies:
  pre:
    - cp .env.development .env.test
    - npm install
    - cd client && npm run build:client

deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy
